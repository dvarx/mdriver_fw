# CPU1 Flash build instructions
To compile the project, run
```bash
$HOME/ti/ccs/utils/bin/gmake -k -j 14 all -O
```
to clean the project run
```bash
$HOME/ti/ccs/utils/bin/gmake clean
```
from this directory